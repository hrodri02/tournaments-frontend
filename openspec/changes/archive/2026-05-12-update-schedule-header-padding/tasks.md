## 1. Verify GameExcerpt horizontal inset

- [x] 1.1 Read `store/leagues/GameExcerpt.tsx` and note the effective horizontal padding/margin used in game rows so the header value can match it exactly.

## 2. Update header padding

- [x] 2.1 In `src/app/(app)/home/leagues/[id]/index.tsx`, update the `header` style inside `StyleSheet.create`: set `paddingLeft` and `paddingRight` to match the horizontal inset identified in task 1.1.
